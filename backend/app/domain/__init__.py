"""Domain layer — pure business model.

Contains entities, value objects and *ports* (abstract interfaces). Has NO
dependency on FastAPI, SQLAlchemy, Redis, OpenCV or PyTorch. Everything here can
be unit-tested in isolation.
"""
