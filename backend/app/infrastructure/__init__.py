"""Infrastructure layer — concrete implementations of domain ports.

Databases, Redis, AI models and external APIs live here. This layer depends on the
domain (to implement its interfaces) but the domain never depends on it.
"""
